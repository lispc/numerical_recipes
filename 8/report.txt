实验要求
	题目为计算矩阵绝对值最大的特征值和对应的特征向量。
	实验数据在代码中。

算法描述
	使用了幂法。
	v(0) ＝ u(0) = {1}
	v(k) = A*u(k-1)
	mu(k) = max(v(k))
	u(k) = v(k)/mu(k)
	
	u(k)极限为所求特征向量，mu(k)极限为所求特征值。

体会心得
	学会了使用二维数组作为参数传递

实验结果
	A在13次迭代后收敛，结果为(0.6740,1.0000,0.8896)和12.2543121
	B在6次迭代后收敛，结果为(0.6040,1.0000,0.2511,0.1490)和98.5216977
